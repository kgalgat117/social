var express = require('express');
var router = express.Router();
var oAuthServer = require('./../config/oauthserver')

var CONSTANTS = require('./../config/contants')

var Miidlewares = require('./../controller/middleware')



module.exports = router;
